{
  "name": "gather-api",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "matsu4ki",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build --compile --target=bun-darwin-arm64 ./index.js --outfile dist/gather-speed-api && rm -rf .*\\.bun-build"
  },
  "dependencies": {
    "@gathertown/gather-game-client": "^43.0.1",
    "isomorphic-ws": "^5.0.0",
    "ws": "^8.18.3"
  }
}
